<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Preços por modelo (USD / 1M tokens)
    |--------------------------------------------------------------------------
    |
    | Legado. O preço vigente de modelo pago está em llm_models
    | (price_input / price_cached / price_output) e é lido por LlmPricing
    | via LlmModelCatalog. Nenhum código de produção lê esta chave.
    |
    */
    'pricing' => [
    ],

];
